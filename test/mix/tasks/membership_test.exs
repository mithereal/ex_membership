defmodule Mix.Tasks.MembershipTest do
  use ExUnit.Case
  ### this doesnt work with circle ci
  #  test "provide a list of available membership mix tasks" do
  #    Mix.Tasks.Membership.run([])
  #    # assert_received {:mix_shell, :info, ["Membership v" <> _]}
  #    assert_received {:mix_shell, :info, ["mix membership.setup" <> _]}
  #  end
end
